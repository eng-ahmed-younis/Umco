<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.UMCOStore" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/colorWhite</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="Theme.UMCOStore" parent="Base.Theme.UMCOStore" />


    <!-- full screen activity -->
    <style name="splash_theme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="android:windowBackground">@drawable/splash_background</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>


    <!-- Primary button -->
    <style name="PrimaryButton">
        <item name="android:background">@drawable/rounded_view</item>
        <item name="textAllCaps">false</item>
        <item name="android:fontFamily">@font/poppins</item>
        <item name="android:backgroundTint">@color/primaryButton</item>
        <item name="android:textColor">@color/colorWhite</item>
    </style>


    <!-- Secondary button -->
    <style name="SecondaryButton">
        <item name="textAllCaps">false</item>
        <item name="android:background">@drawable/rounded_view</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">@dimen/height_large</item>
        <item name="android:fontFamily">@font/poppins</item>
        <item name="android:backgroundTint">@color/secondaryButton</item>
        <item name="android:textColor">@color/items_color_title</item>
    </style>


    <style name="BackButton">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:clickable">true</item>
        <item name="android:focusable">true</item>
        <item name="android:foreground">?attr/selectableItemBackground</item>
        <item name="srcCompat">@drawable/ic_back_btn</item>
    </style>


    <!-- Edittext -->
    <style name="Edittext">
        <item name="android:backgroundTint">@color/background_edittext</item>
        <item name="android:background">@drawable/rounded_view</item>
        <item name="android:fontFamily">@font/poppins</item>
        <item name="android:textSize">14sp</item>
        <item name="android:paddingEnd">16dp</item>
        <item name="android:paddingStart">16dp</item>
    </style>


</resources>